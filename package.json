{
  "scripts": {
    "lint": "eslint --ignore-path .gitignore --ext .js,.ts . --no-error-on-unmatched-pattern",
    "lint:fix": "eslint --ignore-path .gitignore --ext .js,.ts . --fix --no-error-on-unmatched-pattern"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.22.0",
    "@typescript-eslint/parser": "^5.22.0",
    "eslint": "^8.15.0",
    "typescript": "^4.6.4"
  }
}